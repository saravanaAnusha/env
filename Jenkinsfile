pipeline {
    agent any
    stages{
        stage ('Environment') {
            steps {
                echo "NODE_NAME" = ${env.NODE_NAME}"
                sh 'echo NODE_NAME = $NODE_NAME'
        }

    }
}
}
